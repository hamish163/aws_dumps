---
datatype: aws.rds.db_cluster_parameter
metadata:
  profile: default
  region: ap-northeast-1
  resourceid: default.aurora5.6.old_passwords
specifics:
  AllowedValues: 0,2
  ApplyMethod: pending-reboot
  ApplyType: dynamic
  DataType: integer
  Description: Force the server to generate short (pre-4.1) password hashes for new
    passwords. This is useful for compatibility when the server must support older
    client programs.
  IsModifiable: true
  ParameterName: old_passwords
  Source: engine-default
  SupportedEngineModes:
  - provisioned
...

