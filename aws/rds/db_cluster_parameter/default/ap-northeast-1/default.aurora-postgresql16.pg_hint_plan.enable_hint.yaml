---
datatype: aws.rds.db_cluster_parameter
metadata:
  profile: default
  region: ap-northeast-1
  resourceid: default.aurora-postgresql16.pg_hint_plan.enable_hint
specifics:
  AllowedValues: 0,1
  ApplyMethod: pending-reboot
  ApplyType: dynamic
  DataType: boolean
  Description: Force planner to use plans specified in the hint comment preceding
    to the query.
  IsModifiable: true
  ParameterName: pg_hint_plan.enable_hint
  Source: engine-default
  SupportedEngineModes:
  - provisioned
...

