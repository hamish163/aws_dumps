---
datatype: aws.rds.db_cluster_parameter
metadata:
  profile: default
  region: ap-northeast-1
  resourceid: mbx-bnb-perf-commission-attribution-cluster-pg.socket
specifics:
  ApplyMethod: pending-reboot
  ApplyType: static
  DataType: string
  Description: (UNIX) socket file and (WINDOWS) named pipe used for local connections.
  IsModifiable: false
  ParameterName: socket
  ParameterValue: /tmp/mysql.sock
  Source: system
  SupportedEngineModes:
  - provisioned
...

