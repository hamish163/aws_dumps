---
datatype: aws.rds.db_cluster_parameter
metadata:
  profile: default
  region: ap-northeast-1
  resourceid: mbx-bnb-solutions-telegrambot-postgres-cluster-pg.aurora_stat_plans.with_timing
specifics:
  AllowedValues: 0,1
  ApplyMethod: pending-reboot
  ApplyType: dynamic
  DataType: boolean
  Description: Include actual startup time and time spent in each plan node. This
    parameter only takes effect when aurora_stat_plans.with_analyze is also enabled.
  IsModifiable: true
  ParameterName: aurora_stat_plans.with_timing
  ParameterValue: 'on'
  Source: engine-default
  SupportedEngineModes:
  - provisioned
...

