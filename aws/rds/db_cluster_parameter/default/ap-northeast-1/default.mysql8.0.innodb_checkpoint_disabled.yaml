---
datatype: aws.rds.db_cluster_parameter
metadata:
  profile: default
  region: ap-northeast-1
  resourceid: default.mysql8.0.innodb_checkpoint_disabled
specifics:
  AllowedValues: 0,1
  ApplyMethod: pending-reboot
  ApplyType: dynamic
  DataType: boolean
  Description: This is a debug option that is only intended for expert debugging use.
    It disables checkpoints so that a deliberate server exit always initiates InnoDB
    recovery.
  IsModifiable: false
  ParameterName: innodb_checkpoint_disabled
  Source: engine-default
  SupportedEngineModes:
  - provisioned
...

