---
datatype: aws.rds.db_cluster_parameter
metadata:
  profile: default
  region: ap-northeast-1
  resourceid: perf-dev-mbx-orders-cluster-pg-80.binlog_row_metadata
specifics:
  AllowedValues: full,minimal
  ApplyMethod: pending-reboot
  ApplyType: dynamic
  DataType: string
  Description: Configures the amount of table metadata added to the binary log when
    using row-based logging.
  IsModifiable: true
  ParameterName: binlog_row_metadata
  Source: engine-default
  SupportedEngineModes:
  - provisioned
...

