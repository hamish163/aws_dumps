---
datatype: aws.rds.db_cluster_parameter
metadata:
  profile: default
  region: ap-northeast-1
  resourceid: mbx-bnb-solutions-telegrambot-postgres-cluster-pg.max_parallel_apply_workers_per_subscription
specifics:
  AllowedValues: 0-1024
  ApplyMethod: pending-reboot
  ApplyType: dynamic
  DataType: integer
  Description: Maximum number of parallel apply workers per subscription.
  IsModifiable: true
  ParameterName: max_parallel_apply_workers_per_subscription
  ParameterValue: '2'
  Source: engine-default
  SupportedEngineModes:
  - provisioned
...

