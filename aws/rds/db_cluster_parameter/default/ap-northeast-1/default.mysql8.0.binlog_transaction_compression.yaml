---
datatype: aws.rds.db_cluster_parameter
metadata:
  profile: default
  region: ap-northeast-1
  resourceid: default.mysql8.0.binlog_transaction_compression
specifics:
  AllowedValues: 0,1
  ApplyMethod: pending-reboot
  ApplyType: dynamic
  DataType: boolean
  Description: If this variable is turned on, transaction payloads are compressed
    and then written to the binary log as a single event.
  IsModifiable: true
  MinimumEngineVersion: 8.0.20
  ParameterName: binlog_transaction_compression
  Source: engine-default
  SupportedEngineModes:
  - provisioned
...

