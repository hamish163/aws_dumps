---
datatype: aws.rds.db_cluster_parameter
metadata:
  profile: default
  region: ap-northeast-1
  resourceid: mbx-bnb-perf-key-cluster-pg.innodb_deadlock_detect
specifics:
  AllowedValues: ON,OFF
  ApplyMethod: pending-reboot
  ApplyType: dynamic
  DataType: string
  Description: Enable/disable InnoDB deadlock detector (default ON).
  IsModifiable: true
  ParameterName: innodb_deadlock_detect
  ParameterValue: 'ON'
  Source: engine-default
  SupportedEngineModes:
  - provisioned
...

