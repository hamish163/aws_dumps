---
datatype: aws.rds.db_cluster_parameter
metadata:
  profile: default
  region: ap-northeast-1
  resourceid: mbx-bnb-perf-key-cluster-pg.innodb_spin_wait_pause_multiplier
specifics:
  AllowedValues: 1-100
  ApplyMethod: pending-reboot
  ApplyType: dynamic
  DataType: integer
  Description: Defines a multiplier value used to determine the number of PAUSE instructions
    in spin-wait loops that occur when a thread waits to acquire a mutex or rw-lock.
  IsModifiable: true
  ParameterName: innodb_spin_wait_pause_multiplier
  Source: engine-default
  SupportedEngineModes:
  - provisioned
...

