---
datatype: aws.rds.db_cluster_parameter
metadata:
  profile: default
  region: ap-northeast-1
  resourceid: default.aurora-mysql5.7.slave_parallel_type
specifics:
  AllowedValues: DATABASE,LOGICAL_CLOCK
  ApplyMethod: pending-reboot
  ApplyType: dynamic
  DataType: string
  Description: Enable parallel execution on the slave of all uncommitted threads already
    in the prepare phase, without violating consistency.
  IsModifiable: true
  ParameterName: slave_parallel_type
  Source: engine-default
  SupportedEngineModes:
  - provisioned
...

