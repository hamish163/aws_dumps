---
datatype: aws.rds.db_cluster_parameter
metadata:
  profile: default
  region: ap-northeast-1
  resourceid: default.aurora-postgresql17.apg_enable_not_in_transform
specifics:
  AllowedValues: 0,1
  ApplyMethod: pending-reboot
  ApplyType: dynamic
  DataType: boolean
  Description: Enables the planner to transform NOT IN subquery to ANTI JOIN when
    possible.
  IsModifiable: true
  ParameterName: apg_enable_not_in_transform
  Source: engine-default
  SupportedEngineModes:
  - provisioned
...

