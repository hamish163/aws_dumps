---
datatype: aws.rds.db_cluster_parameter
metadata:
  profile: default
  region: ap-northeast-1
  resourceid: default.mysql8.0.mandatory_roles
specifics:
  ApplyMethod: pending-reboot
  ApplyType: dynamic
  DataType: string
  Description: All the specified roles are always considered granted to every user
    and they can't be revoked. Mandatory roles still require activation unless they
    are made into default roles. The granted roles will not be visible in the mysql.role_edges
    table.
  IsModifiable: true
  ParameterName: mandatory_roles
  Source: engine-default
  SupportedEngineModes:
  - provisioned
...

