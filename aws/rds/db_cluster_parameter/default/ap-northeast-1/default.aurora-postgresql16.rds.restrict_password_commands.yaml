---
datatype: aws.rds.db_cluster_parameter
metadata:
  profile: default
  region: ap-northeast-1
  resourceid: default.aurora-postgresql16.rds.restrict_password_commands
specifics:
  AllowedValues: 0,1
  ApplyMethod: pending-reboot
  ApplyType: static
  DataType: boolean
  Description: restricts password-related commands to members of rds_password
  IsModifiable: true
  ParameterName: rds.restrict_password_commands
  Source: engine-default
  SupportedEngineModes:
  - provisioned
...

