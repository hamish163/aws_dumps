---
datatype: aws.rds.db_cluster_parameter
metadata:
  profile: default
  region: ap-northeast-1
  resourceid: default.mysql8.0.binlog_transaction_dependency_tracking
specifics:
  AllowedValues: COMMIT_ORDER,WRITESET,WRITESET_SESSION
  ApplyMethod: pending-reboot
  ApplyType: dynamic
  DataType: list
  Description: Selects the source of dependency information from which to assess which
    transactions can be executed in parallel by the slave's multi-threaded applier.
    Possible values are COMMIT_ORDER, WRITESET and WRITESET_SESSION.
  IsModifiable: false
  ParameterName: binlog_transaction_dependency_tracking
  ParameterValue: WRITESET
  Source: system
  SupportedEngineModes:
  - provisioned
...

