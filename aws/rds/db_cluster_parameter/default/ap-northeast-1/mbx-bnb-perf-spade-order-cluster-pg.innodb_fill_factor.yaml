---
datatype: aws.rds.db_cluster_parameter
metadata:
  profile: default
  region: ap-northeast-1
  resourceid: mbx-bnb-perf-spade-order-cluster-pg.innodb_fill_factor
specifics:
  AllowedValues: 10-100
  ApplyMethod: pending-reboot
  ApplyType: dynamic
  DataType: integer
  Description: Defines the percentage of space on each B-tree page that is filled
    during a sorted index build, with the remaining space reserved for future index
    growth.
  IsModifiable: true
  ParameterName: innodb_fill_factor
  Source: engine-default
  SupportedEngineModes:
  - provisioned
...

