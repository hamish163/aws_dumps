---
datatype: aws.rds.db_cluster_parameter
metadata:
  profile: default
  region: ap-northeast-1
  resourceid: mbx-bnb-perf-key-cluster-pg.binlog_backup
specifics:
  AllowedValues: 0,1
  ApplyMethod: pending-reboot
  ApplyType: static
  DataType: boolean
  Description: This setting controls if binary log data will be backed up or not.
    This setting is only used with aurora_enhanced_binlog. It must be disabled when
    aurora_enhanced_binlog is enabled, and enabled when aurora_enhanced_binlog is
    disabled.
  IsModifiable: true
  MinimumEngineVersion: 8.0.mysql_aurora.3.03.1
  ParameterName: binlog_backup
  ParameterValue: '1'
  Source: system
  SupportedEngineModes:
  - provisioned
...

