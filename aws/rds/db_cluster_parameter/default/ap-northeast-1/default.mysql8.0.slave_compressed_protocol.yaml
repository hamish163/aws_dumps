---
datatype: aws.rds.db_cluster_parameter
metadata:
  profile: default
  region: ap-northeast-1
  resourceid: default.mysql8.0.slave_compressed_protocol
specifics:
  AllowedValues: 0,1
  ApplyMethod: pending-reboot
  ApplyType: dynamic
  DataType: boolean
  Description: Whether to use compression of the slave/master protocol if both the
    slave and the master support it.
  IsModifiable: true
  ParameterName: slave_compressed_protocol
  Source: engine-default
  SupportedEngineModes:
  - provisioned
...

