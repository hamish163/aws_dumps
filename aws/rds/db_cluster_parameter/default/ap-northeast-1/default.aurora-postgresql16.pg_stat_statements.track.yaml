---
datatype: aws.rds.db_cluster_parameter
metadata:
  profile: default
  region: ap-northeast-1
  resourceid: default.aurora-postgresql16.pg_stat_statements.track
specifics:
  AllowedValues: NONE,TOP,ALL
  ApplyMethod: pending-reboot
  ApplyType: dynamic
  DataType: string
  Description: Selects which statements are tracked by pg_stat_statements.
  IsModifiable: true
  ParameterName: pg_stat_statements.track
  Source: engine-default
  SupportedEngineModes:
  - provisioned
...

