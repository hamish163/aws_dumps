---
datatype: aws.rds.db_cluster_parameter
metadata:
  profile: default
  region: ap-northeast-1
  resourceid: default.aurora-postgresql17.pgtle.passcheck_db_name
specifics:
  ApplyMethod: pending-reboot
  ApplyType: dynamic
  DataType: string
  Description: If set, controls which database is used for cluster-wide passcheck
    feature.
  IsModifiable: true
  ParameterName: pgtle.passcheck_db_name
  Source: engine-default
  SupportedEngineModes:
  - provisioned
...

