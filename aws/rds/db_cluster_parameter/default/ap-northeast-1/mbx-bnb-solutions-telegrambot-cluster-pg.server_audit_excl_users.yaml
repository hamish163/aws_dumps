---
datatype: aws.rds.db_cluster_parameter
metadata:
  profile: default
  region: ap-northeast-1
  resourceid: mbx-bnb-solutions-telegrambot-cluster-pg.server_audit_excl_users
specifics:
  ApplyMethod: pending-reboot
  ApplyType: dynamic
  DataType: string
  Description: If not empty, it contains the list of users whose activity will NOT
    be logged
  IsModifiable: true
  ParameterName: server_audit_excl_users
  Source: engine-default
  SupportedEngineModes:
  - provisioned
...

