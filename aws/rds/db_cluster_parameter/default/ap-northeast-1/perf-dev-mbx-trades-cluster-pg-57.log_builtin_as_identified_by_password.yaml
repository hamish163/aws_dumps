---
datatype: aws.rds.db_cluster_parameter
metadata:
  profile: default
  region: ap-northeast-1
  resourceid: perf-dev-mbx-trades-cluster-pg-57.log_builtin_as_identified_by_password
specifics:
  AllowedValues: 0,1
  ApplyMethod: pending-reboot
  ApplyType: dynamic
  DataType: boolean
  Description: If enabled, binary logging for CREATE USER statements involving built-in
    authentication plugins rewrites the statements to include an IDENTIFIED BY PASSWORD
    clause, and SET PASSWORD statements are logged as SET PASSWORD statements, rather
    than being rewritten to ALTER USER statements.
  IsModifiable: true
  ParameterName: log_builtin_as_identified_by_password
  Source: engine-default
  SupportedEngineModes:
  - provisioned
...

