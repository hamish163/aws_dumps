---
datatype: aws.rds.db_cluster_parameter
metadata:
  profile: default
  region: ap-northeast-1
  resourceid: mbx-bnb-perf-kline-cluster-pg.binlog_transaction_compression_level_zstd
specifics:
  AllowedValues: 1-22
  ApplyMethod: pending-reboot
  ApplyType: dynamic
  DataType: integer
  Description: Sets the compression level for binary log transaction compression on
    this server, which is enabled by the binlog_transaction_compression system variable.
  IsModifiable: true
  ParameterName: binlog_transaction_compression_level_zstd
  Source: engine-default
  SupportedEngineModes:
  - provisioned
...

