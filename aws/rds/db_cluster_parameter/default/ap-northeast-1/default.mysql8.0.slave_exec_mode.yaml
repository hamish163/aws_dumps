---
datatype: aws.rds.db_cluster_parameter
metadata:
  profile: default
  region: ap-northeast-1
  resourceid: default.mysql8.0.slave_exec_mode
specifics:
  AllowedValues: IDEMPOTENT,STRICT
  ApplyMethod: pending-reboot
  ApplyType: dynamic
  DataType: string
  Description: slave_exec_mode controls how a replication thread resolves conflicts
    and errors during replication.
  IsModifiable: false
  ParameterName: slave_exec_mode
  ParameterValue: STRICT
  Source: engine-default
  SupportedEngineModes:
  - provisioned
...

