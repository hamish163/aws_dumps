---
datatype: aws.rds.db_cluster_parameter
metadata:
  profile: default
  region: ap-northeast-1
  resourceid: perf-dev-mbx-accounts-cluster-pg-57.performance-schema-instrument
specifics:
  ApplyMethod: pending-reboot
  ApplyType: static
  DataType: string
  Description: Configure a Performance Schema instrument. The name may be given as
    a pattern to configure instruments that match the pattern.
  IsModifiable: true
  ParameterName: performance-schema-instrument
  Source: engine-default
  SupportedEngineModes:
  - provisioned
...

