---
datatype: aws.rds.db_cluster_parameter
metadata:
  profile: default
  region: ap-northeast-1
  resourceid: mbx-bnb-solutions-telegrambot-postgres-cluster-pg.max_parallel_workers_per_gather
specifics:
  AllowedValues: 0-1024
  ApplyMethod: pending-reboot
  ApplyType: dynamic
  DataType: integer
  Description: Sets the maximum number of parallel processes per executor node.
  IsModifiable: true
  ParameterName: max_parallel_workers_per_gather
  Source: engine-default
  SupportedEngineModes:
  - provisioned
...

