---
datatype: aws.rds.db_cluster_parameter
metadata:
  profile: default
  region: ap-northeast-1
  resourceid: default.aurora5.6.binlog_checksum
specifics:
  AllowedValues: NONE,CRC32
  ApplyMethod: pending-reboot
  ApplyType: dynamic
  DataType: string
  Description: When enabled, this variable causes the master to write a checksum for
    each event in the binary log.
  IsModifiable: true
  ParameterName: binlog_checksum
  Source: engine-default
  SupportedEngineModes:
  - provisioned
...

