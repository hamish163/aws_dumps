---
datatype: aws.rds.db_cluster_parameter
metadata:
  profile: default
  region: ap-northeast-1
  resourceid: perf-dev-mbx-system-cluster-pg-57.sync_frm
specifics:
  AllowedValues: 0,1
  ApplyMethod: pending-reboot
  ApplyType: dynamic
  DataType: boolean
  Description: For non-temporary tables controls if .frm file is sync'ed to disk.
  IsModifiable: true
  ParameterName: sync_frm
  Source: engine-default
  SupportedEngineModes:
  - provisioned
...

