---
datatype: aws.rds.db_cluster_parameter
metadata:
  profile: default
  region: ap-northeast-1
  resourceid: perf-dev57-mbx-orders-cluster-pgrp.innodb_compression_level
specifics:
  AllowedValues: 0-9
  ApplyMethod: pending-reboot
  ApplyType: dynamic
  DataType: integer
  Description: Sets the cutoff point at which MySQL begins adding padding within compressed
    pages to avoid expensive compression failures.
  IsModifiable: true
  ParameterName: innodb_compression_level
  Source: engine-default
  SupportedEngineModes:
  - provisioned
...

