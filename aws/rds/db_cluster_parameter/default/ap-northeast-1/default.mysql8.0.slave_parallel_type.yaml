---
datatype: aws.rds.db_cluster_parameter
metadata:
  profile: default
  region: ap-northeast-1
  resourceid: default.mysql8.0.slave_parallel_type
specifics:
  AllowedValues: DATABASE,LOGICAL_CLOCK
  ApplyMethod: pending-reboot
  ApplyType: static
  DataType: string
  Description: When using a multithreaded slave (slave_parallel_workers is greater
    than 0), this variable specifies the policy used to decide which transactions
    are allowed to execute in parallel on the slave.
  IsModifiable: false
  ParameterName: slave_parallel_type
  ParameterValue: LOGICAL_CLOCK
  Source: system
  SupportedEngineModes:
  - provisioned
...

