---
datatype: aws.rds.db_cluster_parameter
metadata:
  profile: default
  region: ap-northeast-1
  resourceid: mbx-bnb-uat-aggtrade-cluster-pg.server_audit_incl_users
specifics:
  ApplyMethod: pending-reboot
  ApplyType: dynamic
  DataType: string
  Description: If not empty, it contains a comma-delimited list of users whose activity
    will be logged
  IsModifiable: true
  ParameterName: server_audit_incl_users
  Source: engine-default
  SupportedEngineModes:
  - provisioned
...

