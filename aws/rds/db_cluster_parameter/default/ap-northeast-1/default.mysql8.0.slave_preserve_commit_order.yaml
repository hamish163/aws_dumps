---
datatype: aws.rds.db_cluster_parameter
metadata:
  profile: default
  region: ap-northeast-1
  resourceid: default.mysql8.0.slave_preserve_commit_order
specifics:
  AllowedValues: 0,1
  ApplyMethod: pending-reboot
  ApplyType: dynamic
  DataType: boolean
  Description: Ensures that transactions are executed and committed on the replica
    in the same order as they appear in the relay log.
  IsModifiable: true
  ParameterName: slave_preserve_commit_order
  ParameterValue: '0'
  Source: system
  SupportedEngineModes:
  - provisioned
...

