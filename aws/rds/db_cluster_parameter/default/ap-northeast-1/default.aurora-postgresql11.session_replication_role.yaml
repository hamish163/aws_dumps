---
datatype: aws.rds.db_cluster_parameter
metadata:
  profile: default
  region: ap-northeast-1
  resourceid: default.aurora-postgresql11.session_replication_role
specifics:
  AllowedValues: origin,replica,local
  ApplyMethod: pending-reboot
  ApplyType: dynamic
  DataType: string
  Description: Sets the sessions behavior for triggers and rewrite rules.
  IsModifiable: true
  ParameterName: session_replication_role
  Source: engine-default
  SupportedEngineModes:
  - provisioned
  - serverless
...

