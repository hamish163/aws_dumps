---
datatype: aws.rds.db_cluster_parameter
metadata:
  profile: default
  region: ap-northeast-1
  resourceid: default.aurora-postgresql17.wal_receiver_create_temp_slot
specifics:
  AllowedValues: 0,1
  ApplyMethod: pending-reboot
  ApplyType: dynamic
  DataType: boolean
  Description: Sets whether a WAL receiver should create a temporary replication slot
    if no permanent slot is configured.
  IsModifiable: false
  ParameterName: wal_receiver_create_temp_slot
  ParameterValue: '0'
  Source: engine-default
  SupportedEngineModes:
  - provisioned
...

