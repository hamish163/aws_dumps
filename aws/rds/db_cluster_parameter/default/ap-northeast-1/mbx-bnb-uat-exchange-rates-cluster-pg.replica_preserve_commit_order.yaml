---
datatype: aws.rds.db_cluster_parameter
metadata:
  profile: default
  region: ap-northeast-1
  resourceid: mbx-bnb-uat-exchange-rates-cluster-pg.replica_preserve_commit_order
specifics:
  AllowedValues: ON,OFF
  ApplyMethod: pending-reboot
  ApplyType: dynamic
  DataType: string
  Description: For a replica with binary logging enabled, setting replica_preserve_commit_order=1
    guarantees transactions are written to the replica binary log in the same order
    they appear in the relay logs
  IsModifiable: true
  ParameterName: replica_preserve_commit_order
  Source: engine-default
  SupportedEngineModes:
  - provisioned
...

