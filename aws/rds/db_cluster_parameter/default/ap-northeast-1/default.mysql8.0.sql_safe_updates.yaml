---
datatype: aws.rds.db_cluster_parameter
metadata:
  profile: default
  region: ap-northeast-1
  resourceid: default.mysql8.0.sql_safe_updates
specifics:
  AllowedValues: 0,1
  ApplyMethod: pending-reboot
  ApplyType: dynamic
  DataType: boolean
  Description: If this variable is enabled, UPDATE and DELETE statements that do not
    use a key in the WHERE clause or a LIMIT clause produce an error.
  IsModifiable: true
  ParameterName: sql_safe_updates
  ParameterValue: '1'
  Source: system
  SupportedEngineModes:
  - provisioned
...

